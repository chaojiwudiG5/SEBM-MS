server:
  port: 8083  # borrow-service 独立端口

spring:
  application:
    name: borrow-service  # 服务名
  datasource:
    url: jdbc:mysql://trolley.proxy.rlwy.net:29578/railway?useSSL=false&serverTimezone=UTC
    username: root
    password: RxAHOOQJjTtBblTpadijgyIWmArpvibI
    driver-class-name: com.mysql.cj.jdbc.Driver
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848  # 注册中心地址
      config:
        server-addr: localhost:8848
        file-extension: yaml  # 如果要拉取配置中心配置

dubbo:
  application:
    name: borrow-service  # Dubbo 应用名
  registry:
    address: nacos://localhost:8848
  protocol:
    name: dubbo
    port: -1  # 随机端口，避免冲突
